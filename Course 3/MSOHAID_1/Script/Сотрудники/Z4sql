-- Создание объектного представления
CREATE OR REPLACE VIEW Сотрудники_view AS
SELECT * FROM Сотрудники_Obj;


-- Использование объектного представления в запросе
SELECT v.ФИО_СОТРУДНИКА, v.РОЛЬ
FROM Сотрудники_view v
WHERE v.РОЛЬ = 'Менеджер';
